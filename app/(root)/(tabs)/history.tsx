import { Text, View} from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const History = () =>{
return(
    <SafeAreaView>
      <Text className="text-lg font-bold text-blue-500">History</Text>
      </SafeAreaView>
)
}

export default History;